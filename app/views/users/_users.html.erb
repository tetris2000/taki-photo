<ul>
  <% users.each do |u| %>
    <li class="media m-2">
      <%= cl_image_tag(u.icon_photo, :width=>100, :height=>100, :gravity=>"auto", :crop=>"fill", :alt=>"icon") %>
      <div class="media-body">
        <%= link_to u.name, user_path(u) %>
      </div>
    </li>
  <% end %>
</ul>